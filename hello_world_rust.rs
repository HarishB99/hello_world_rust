fn main() {
    println!("An unusual way to create a hello world project.");
    println!("Cargo.toml was manually created.");
    println!("The source file was manually configured in Cargo.toml.");
    println!("`cargo new/cargo init` was not used.");
    println!("Instead of creating main.rs, hello_world_rust.rs was created. (which is me by the way)");
}
